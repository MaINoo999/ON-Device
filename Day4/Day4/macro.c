//#include <stdio.h>  
// 표준 입출력 라이브러리. printf 함수를 사용하기 위해 반드시 포함해야 함.


// 매크로 함수 SUM 정의
// 전달된 두 인자 a, b를 더한 값을 반환하는 식을 매크로로 정의
// 괄호로 감싸는 이유는 연산자 우선순위 문제를 방지하기 위함
//#define SUM(a,b)  ((a)+(b))	


// 매크로 함수 MUL 정의
// 전달된 두 인자 a, b를 곱한 값을 반환하는 식
// 마찬가지로 괄호를 사용하여 안전하게 사용 가능하게 함
//#define MUL(a,b)  ((a)*(b))


//int main() {
    // 정수형 변수 선언 및 초기화
//	int a = 10, b = 20;
//	int x = 30, y = 40;
//	int res; // 결과값을 저장할 변수 (예제에서는 사용하지 않았지만 남겨둠)

    // SUM(a, b) → ((10)+(20)) → 30
//	printf("a+b= %d\n", SUM(a, b));

    // SUM(x, y) → ((30)+(40)) → 70
//	printf("x+y= %d\n", SUM(x, y));

    // MUL(a, b) → ((10)*(20)) → 200
//	printf("a*b= %d\n", MUL(a, b));

    // 프로그램 정상 종료
//	return 0;
//}
