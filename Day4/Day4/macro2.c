//#include <stdio.h> // 표준 입출력 헤더 파일 포함. printf() 함수 사용을 위해 필요.
//
//// 매크로 정의: VER을 7로 정의함.
//#define VER 7
//
//// 매크로 정의: BIT16을 정의함. 값은 명시하지 않았지만, 정의 유무만으로 조건 판단에 사용됨.
//#define BIT16
//
//int main() {
//
//    int max; // 정수형 변수 max 선언
//
//    // 조건부 컴파일: VER이 6 이상일 경우에만 아래 코드 블록이 컴파일됨
//#if VER >= 6
//    // VER 매크로 값이 6 이상일 때만 출력됨
//    printf("버전 %d 입니다.\n", VER);
//#endif
//
//    // 조건부 컴파일: BIT16이 정의되어 있으면 아래 코드 블록 실행
//#ifdef BIT16
//    max = 32767; // 16비트 정수의 최대값 (short형 최대값에 해당)
//
//    // BIT16이 정의되어 있지 않은 경우 실행
//#else
//    max = 2147483647; // 32비트 정수의 최대값 (int형 최대값)
//#endif // BIT16
//
//    // 최대값 출력
//    printf("int 형 변수 최대값: %d\n", max);
//
//    return 0; // 프로그램 정상 종료
//}
