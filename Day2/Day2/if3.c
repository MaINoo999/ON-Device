/* if ~ else id ~ else */
/*
#include <stdio.h>

   출력버퍼
   입력버퍼 : 값을 입력하는순간 저장되는곳 scanf를 사용시 입력버퍼에 있는것을 가져온다.



void main() {

	char ch;

	printf("Input char acter: ");
	ch = getchar();									//입력버퍼에서 문자 하나를 가져오는 입력함수.

	if ((ch >= 'a' && ch <= 'z') || (ch >= 'A') && (ch <= 'Z')) printf("alphabet\n");				// a보다 크거나 z보다 작을때 알파뱃 나타냄
	else if (ch >= '0' && ch <= '9') printf("numeric");
	else { printf("others\n"); }


}
*/